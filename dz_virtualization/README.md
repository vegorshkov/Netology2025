======================================================================================
Задача 2:

Выберите один из вариантов платформы

- высоконагруженная база данных MySql, критичная к отказу;
- выбираем:
виртуализация уровня ОС;
*объяснение:
выбор сделан потому что не требуется создания привязки машин к железу, при этом обеспечивается лучшая производительность, так как это не программная виртуализация, и в том числе, при отказе ОС гипервизора мы сможем спокойно перенести, мигрировать виртуалку на другой гипервизор.

- различные web-приложения;
Windows-системы для использования бухгалтерским отделом;
- выбираем:
паравиртуализация;
*объяснение:
выбор сделан потому что это вид программной виртуализации, при отказе, время восстановления будет минимальным и сам процесс довольно простым. Так же контейнеры будут изолированы друг от друга, как и в других типах виртуализации.

- системы, выполняющие высокопроизводительные расчёты на GPU.
- выбираем:
физические сервера;
*объяснение:
выбор сделан потому что GPU hardware используются и взаимодействуют с виртуальными вычислительными машинами, прямое взаимог
действие с "железом" дает наилучшую производительность и меньший latency.

========================================================================================

Задание 3:

Выберите подходящую систему управления виртуализацией для предложенного сценария. Опишите ваш выбор.

Сценарии:

1. 100 виртуальных машин на базе Linux и Windows, общие задачи, нет особых требований. Преимущественно Windows based-инфраструктура, требуется реализация программных балансировщиков нагрузки, репликации данных и автоматизированного механизма создания резервных копий.
Ответ:
vSphere VMWare ESXi виртуализация c функционалом vMotion или Hyper-V Live migration.

2. Требуется наиболее производительное бесплатное open source-решение для виртуализации небольшой (20-30 серверов) инфраструктуры на базе Linux и Windows виртуальных машин.
Ответ:
KVM - это бесплатный, он предок Redhat virtualization или oVirt. Реализует функционал миграции виртуальных машин,  оно так же как и ware и hyper-v использует общее хранилище и поддерживает функционал Live Migration. 

3. Необходимо бесплатное, максимально совместимое и производительное решение для виртуализации Windows-инфраструктуры.
Ответ:
Hyper-V  - оптимально подходит для виртуализации, так же он лицензируется в составе серверной лицензии как роль виндовс, поддерживает снапшоты, миграции, много microsoft систем управления VMM, SystemCentre и других Linux совместимых для V2V миграций.

4. Необходимо рабочее окружение для тестирования программного продукта на нескольких дистрибутивах Linux.
Ответ:
Docker - функционал docker compose максимально приближен к требованиям тестирования на различных платформах дистрибутивов linux, а так же при  указании версионности lasted (последний релиз) или вводе преведущих номеров версии релиза, будет произведена настройка именно на данной версии,  что соответствует требованиям тестирования. Значение так же  имеет, способность стандартизировать подключения между контейнерами, то есть если в докер-компос прописать правильные порты подключения, ip адреса или другие параметры прописанные в конфиге, то они максимально сохранятся. 

Задача 4
Опишите возможные проблемы и недостатки гетерогенной среды виртуализации (использования нескольких систем управления виртуализацией одновременно) и что необходимо сделать для минимизации этих рисков и проблем. Если бы у вас был выбор, создавали бы вы гетерогенную среду или нет?
Ответ:
Гетерогенная среда, представляет разные системы, в  том числе и системы их управления. Получается, что администратор должен быть высоко-эрудированным специалистом по каждой системе виртуализации.
Обычные средствамиграции виртуальных рессурсов между системами - ограничено, приходится производить переформатирование или перевод в другой формат настроек виртуальных машин, дисков, а так же драверов взаимодействия внутри виртуальны (гостевых) машин.
